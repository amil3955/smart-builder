// This store manages the core building properties  like dimensions, roof style, etc
import { create} from 'zustand';
const useBuildingStore = create((set) => ({
    
    
}));
export default useBuildingStore